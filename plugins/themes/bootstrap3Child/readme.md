# Bootstrap3 Child Theme

A child theme of the PKP Bootstrap3 theme containing styling customizations.

## Notes

- Loads two stylesheets: `styles/custom.less` (site styling) and
  `styles/openid.less` (OpenID plugin login page).
- Set as a child of `bootstrapthreethemeplugin` via `setParent()`.
