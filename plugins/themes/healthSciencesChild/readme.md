# Health Sciences Child Theme

A child theme of the PKP Health Sciences theme containing styling customizations.

## Notes

- Loads two stylesheets: `styles/custom.less` (site styling) and
  `styles/openid.less` (OpenID plugin login page).
- Set as a child of `healthsciencesthemeplugin` via `setParent()`.